# Release Notes

## v0.2.0 (current)

### Added
- Android app module `app` with:
  - browser-like main screen (`WebView`, URL bar, Back/Forward)
  - explicit AI button in toolbar
  - visible AI BottomSheet entry flow
  - dedicated Settings screen
- APK build script update (`scripts/build_android_apk.sh`) with gradlew/system-gradle fallback.

### Changed
- Updated docs to reflect Android source availability and APK build path.

## v0.1.1

### Added
- AI core scaffolding (`aiassistant-core`) + tests.
- Persian documentation and release packaging script.
